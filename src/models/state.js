export default class {}

  class State {
    constructor(stages, currentStageId, player) {
      this.stages = stages
      this.currentStageId = currentStageId
      this.player = player
    }
  }

module.exports = {
  State
}
